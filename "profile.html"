
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>SONICGIOS EMPIRE</title>
  <link rel="icon" type="image/webp" href="https://res.cloudinary.com/wfcrf1ps/image/upload/f_auto,q_auto/WhatsApp_Image_2026-08-15_at_3.27.38_PM">
  <style>
    :root {
      --gold: #D4AF37;
      --gold-light: #F4D03F;
      --gold-dark: #B8860B;
      --black: #0A0A0A;
      --black-card: #1A1A1A;
      --text: #E5E5E5;
      --text-muted: #888888;
    }
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body {
      background: var(--black);
      color: var(--text);
      font-family: 'Segoe UI', system-ui, sans-serif;
      min-height: 100vh;
    }
    .empire-container { max-width: 900px; margin: 0 auto; padding: 24px; }
    .empire-header {
      text-align: center;
      padding: 40px 20px;
      position: relative;
    }
    .empire-header::before {
      content: '';
      position: absolute;
      top: 0; left: 50%;
      transform: translateX(-50%);
      width: 200px; height: 2px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
    }
    .logo-wrap {
      width: 180px;
      height: 180px;
      margin: 0 auto 20px;
      border-radius: 50%;
      border: 3px solid var(--gold);
      overflow: hidden;
      box-shadow: 0 0 40px rgba(212,175,55,0.3), inset 0 0 40px rgba(212,175,55,0.1);
      display: block;
      transition: box-shadow 0.4s ease, transform 0.4s ease;
    }
    .logo-wrap:hover {
      box-shadow: 0 0 60px rgba(212,175,55,0.6), inset 0 0 40px rgba(212,175,55,0.2);
      transform: scale(1.05);
    }
    .logo-wrap img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      display: block;
    }
    .empire-title {
      font-size: 2.8rem;
      font-weight: 900;
      letter-spacing: 6px;
      text-transform: uppercase;
      background: linear-gradient(180deg, var(--gold-light), var(--gold), var(--gold-dark));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      margin-bottom: 8px;
    }
    .empire-subtitle {
      font-size: 1rem;
      color: var(--text-muted);
      letter-spacing: 8px;
      text-transform: uppercase;
    }
    .crown-divider {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 16px;
      margin: 24px 0;
    }
    .crown-divider .line {
      flex: 1;
      height: 1px;
      background: linear-gradient(90deg, transparent, var(--gold-dark), transparent);
      max-width: 120px;
    }
    .crown-divider .crown { font-size: 1.4rem; color: var(--gold); }
    .empire-nav {
      display: flex;
      justify-content: center;
      gap: 8px;
      margin-bottom: 32px;
      flex-wrap: wrap;
    }
    .nav-btn {
      background: var(--black-card);
      border: 1px solid var(--gold-dark);
      color: var(--gold);
      padding: 10px 24px;
      border-radius: 8px;
      cursor: pointer;
      font-size: 0.85rem;
      font-weight: 600;
      letter-spacing: 2px;
      text-transform: uppercase;
      transition: all 0.3s ease;
    }
    .nav-btn:hover, .nav-btn.active {
      background: var(--gold);
      color: var(--black);
      box-shadow: 0 0 20px rgba(212,175,55,0.4);
    }
    .section { display: none; animation: fadeIn 0.5s ease; }
    .section.active { display: block; }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(16px); }
      to { opacity: 1; transform: translateY(0); }
    }
    .card-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
      gap: 20px;
    }
    .empire-card {
      background: var(--black-card);
      border: 1px solid rgba(212,175,55,0.15);
      border-radius: 16px;
      padding: 28px;
      transition: all 0.4s ease;
      position: relative;
      overflow: hidden;
    }
    .empire-card::before {
      content: '';
      position: absolute;
      top: 0; left: 0; right: 0;
      height: 2px;
      background: linear-gradient(90deg, transparent, var(--gold), transparent);
      opacity: 0;
      transition: opacity 0.4s ease;
    }
    .empire-card:hover {
      border-color: var(--gold-dark);
      transform: translateY(-4px);
      box-shadow: 0 12px 40px rgba(0,0,0,0.5), 0 0 20px rgba(212,175,55,0.1);
    }
    .empire-card:hover::before { opacity: 1; }
    .card-icon { font-size: 2rem; margin-bottom: 12px; }
    .card-title {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--gold-light);
      margin-bottom: 8px;
      letter-spacing: 1px;
    }
    .card-desc {
      font-size: 0.9rem;
      color: var(--text-muted);
      line-height: 1.6;
    }
    .card-stat {
      display: inline-block;
      margin-top: 12px;
      padding: 4px 12px;
      background: rgba(212,175,55,0.1);
      border: 1px solid var(--gold-dark);
      border-radius: 20px;
      font-size: 0.75rem;
      color: var(--gold);
      font-weight: 600;
    }
    .about-content {
      background: var(--black-card);
      border-radius: 16px;
      padding: 32px;
      border: 1px solid rgba(212,175,55,0.15);
      line-height: 1.8;
      color: var(--text-muted);
    }
    .about-content p { margin-bottom: 16px; }
    .about-content strong { color: var(--gold-light); }
    .contact-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
      gap: 16px;
    }
    .contact-item {
      background: var(--black-card);
      border: 1px solid rgba(212,175,55,0.15);
      border-radius: 12px;
      padding: 20px;
      text-align: center;
      transition: all 0.3s ease;
      cursor: pointer;
      text-decoration: none;
      display: block;
      color: inherit;
    }
    .contact-item:hover {
      border-color: var(--gold);
      transform: translateY(-2px);
    }
    .contact-item .icon { font-size: 1.6rem; margin-bottom: 8px; display: block; }
    .contact-item .label {
      font-size: 0.8rem;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 2px;
    }
    .contact-item .value {
      font-size: 0.95rem;
      color: var(--gold);
      font-weight: 600;
      margin-top: 4px;
    }
    .empire-footer {
      text-align: center;
      padding: 40px 20px;
      color: var(--text-muted);
      font-size: 0.8rem;
      letter-spacing: 2px;
    }
    .empire-footer .crown {
      color: var(--gold-dark);
      font-size: 1.2rem;
      margin-bottom: 8px;
      display: block;
    }
    @media (max-width: 600px) {
      .empire-title { font-size: 2rem; letter-spacing: 3px; }
      .logo-wrap { width: 140px; height: 140px; }
      .empire-nav { gap: 6px; }
      .nav-btn { padding: 8px 16px; font-size: 0.75rem; }
    }
  </style>
</head>
<body>
  <div class="empire-container">
    <header class="empire-header">
      <a href="https://res.cloudinary.com/wfcrf1ps/image/upload/f_auto,q_auto/WhatsApp_Image_2026-08-15_at_3.27.38_PM" target="_blank" class="logo-wrap" title="View full logo">
        <img src="https://res.cloudinary.com/wfcrf1ps/image/upload/f_auto,q_auto/WhatsApp_Image_2026-08-15_at_3.27.38_PM" alt="SONICGIOS EMPIRE Logo">
      </a>
      <h1 class="empire-title">SONICGIOS</h1>
      <p class="empire-subtitle">Empire Portfolio</p>
      <div class="crown-divider">
        <div class="line"></div>
        <span class="crown">👑</span>
        <div class="line"></div>
      </div>
    </header>

    <nav class="empire-nav">
      <button class="nav-btn active" onclick="showSection('work')">Work</button>
      <button class="nav-btn" onclick="showSection('about')">About</button>
      <button class="nav-btn" onclick="showSection('contact')">Contact</button>
    </nav>

    <section id="work" class="section active">
      <div class="card-grid">
        <div class="empire-card">
          <div class="card-icon">💰</div>
          <div class="card-title">Financial Ventures</div>
          <div class="card-desc">Strategic investments and wealth-building initiatives across multiple markets and asset classes.</div>
          <span class="card-stat">$2.4M+ Managed</span>
        </div>
        <div class="empire-card">
          <div class="card-icon">🎵</div>
          <div class="card-title">Music Production</div>
          <div class="card-desc">Original beats, mixing, and mastering services for artists seeking that signature golden sound.</div>
          <span class="card-stat">150+ Tracks</span>
        </div>
        <div class="empire-card">
          <div class="card-icon">📱</div>
          <div class="card-title">Digital Branding</div>
          <div class="card-desc">Custom logos, visual identities, and social media assets that command attention and respect.</div>
          <span class="card-stat">80+ Clients</span>
        </div>
        <div class="empire-card">
          <div class="card-icon">👕</div>
          <div class="card-title">Merchandise Line</div>
          <div class="card-desc">Premium streetwear and accessories embodying the hustle, ambition, and crown mentality.</div>
          <span class="card-stat">3 Collections</span>
        </div>
        <div class="empire-card">
          <div class="card-icon">📈</div>
          <div class="card-title">Crypto & NFTs</div>
          <div class="card-desc">Blockchain ventures, NFT collections, and decentralized finance strategy consulting.</div>
          <span class="card-stat">5 Projects</span>
        </div>
        <div class="empire-card">
          <div class="card-icon">🎬</div>
          <div class="card-title">Content Creation</div>
          <div class="card-desc">High-impact video content, storytelling, and brand narratives for the digital age.</div>
          <span class="card-stat">1M+ Views</span>
        </div>
      </div>
    </section>

    <section id="about" class="section">
      <div class="about-content">
        <p><strong>The Empire Mindset</strong> — Built from the ground up with an unwavering commitment to excellence. SONICGIOS EMPIRE represents more than a brand; it's a movement rooted in ambition, discipline, and the relentless pursuit of greatness.</p>
        <p>Every project undertaken is stamped with the golden standard — quality over quantity, impact over noise. From the studio to the boardroom, the empire continues to expand its influence across industries.</p>
        <p><strong>Mission:</strong> To inspire, create, and dominate. To turn visions into empires and dreams into dividends. The crown isn't given — it's earned.</p>
      </div>
    </section>

    <section id="contact" class="section">
      <div class="contact-grid">
        <a href="mailto:contact@sonicgios.com" class="contact-item">
          <span class="icon">✉️</span>
          <div class="label">Email</div>
          <div class="value">contact@sonicgios.com</div>
        </a>
        <a href="#" class="contact-item">
          <span class="icon">📸</span>
          <div class="label">Instagram</div>
          <div class="value">@sonicgios</div>
        </a>
        <a href="#" class="contact-item">
          <span class="icon">🐦</span>
          <div class="label">Twitter / X</div>
          <div class="value">@sonicgios</div>
        </a>
        <a href="#" class="contact-item">
          <span class="icon">💬</span>
          <div class="label">WhatsApp</div>
          <div class="value">+1 (555) 019-2834</div>
        </a>
      </div>
    </section>

    <footer class="empire-footer">
      <span class="crown">👑</span>
      <p>SONICGIOS EMPIRE © 2026</p>
      <p style="margin-top:4px; font-size:0.7rem;">ALL RIGHTS RESERVED</p>
    </footer>
  </div>

  <script>
    function showSection(id) {
      document.querySelectorAll('.section').forEach(s => s.classList.remove('active'));
      document.querySelectorAll('.nav-btn').forEach(b => b.classList.remove('active'));
      document.getElementById(id).classList.add('active');
      event.target.classList.add('active');
    }
  </script>
</body>
</html>
